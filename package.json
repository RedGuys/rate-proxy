{
  "name": "rate-proxy",
  "version": "1.0.4",
  "description": "rate-proxy is a simple, configurable proxy server built with Node.js. It allows you to define routes and map them to different endpoints. It also supports rate limiting with a configurable timeout.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "bin": "./index.js",
  "dependencies": {
    "commander": "^10.0.1",
    "express": "^5.0.0"
  }
}
